{% extends 'base.html.twig' %}

{% block title %}Noticia{% endblock %}

{% block body %}
    <h1>Noticia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ noticium.id }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ noticium.Titulo }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ noticium.date ? noticium.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ noticium.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_noticia_index') }}">back to list</a>

    <a href="{{ path('app_noticia_edit', {'id': noticium.id}) }}">edit</a>

    {{ include('noticia/_delete_form.html.twig') }}
{% endblock %}
